import React from "react";

const Profile = () => {
  // Handle input
  const handleChange = () => {};

  // Handle click

  // get user url

  const HandleClick = () => {};
  return (
    <div>
      {/* <label>UPLOAD</label>
      <input type="file" onChange={handleChange} />
      <button onClick={HandleClick}>Upload</button>
      <img
        src="https://www.beelights.gr/assets/images/empty-image.png"
        alt=""
      /> */}
    </div>
  );
};

export default Profile;
